// vitest stub for the Next.js `server-only` package, which throws when imported
// outside a React Server Component. Tests run in a plain Node environment, so
// vitest.config.ts aliases `server-only` to this no-op module.
export {};
